apiVersion: v1
kind: Service

metadata:
  name: emailservice

spec:
  type: {{ .Values.service.type }}

  selector:
    app: emailservice

  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
